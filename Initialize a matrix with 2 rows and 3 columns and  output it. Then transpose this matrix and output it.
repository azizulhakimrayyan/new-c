#include <stdio.h>


int main() {
  int max = 0;
  int r, c;
  int a[2][3]={{1,2,3},{4,5,6}};
for (int i = 0; i < 2; i++)
{
for (int p = 0; p < 3; p++)
{
  printf("%d  ", a[i][p]);
  a[p][i] = a[i][p];
}
printf("\n");
}printf("\n");
for (int i = 0; i < 3; i++)
{
for (int p = 0; p < 2; p++)
{
  printf("%d  ", a[p][i]);
 
}
printf("\n");
}

return 0;
}
