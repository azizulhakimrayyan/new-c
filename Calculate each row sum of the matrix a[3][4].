#include <stdio.h>


int main() {
  int sum1 = 0;
 int sum2 = 0;
  int a[2][3]={{1,2,3},{4,5,6}};
for (int i = 0; i < 3; i++)
{
  sum1 = sum1 + a[0][i];
  sum2 = sum2 + a[1][i];
}
printf("row 1 :%d\n", sum1);
printf("row 2 :%d", sum2);
return 0;
}
