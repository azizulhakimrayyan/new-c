#include <stdio.h>


int main() {
  int x;
  int num=0,r;
  printf("enter a number");
  scanf("%d", &x);
  while (x!=0)
  {
    r=x%10;
    num = num * 10 + r;
    x = x / 10;
  }
  printf("%d", num);

  return 0;
}

